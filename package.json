{
  "name": "chromoskein",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "timeseries-dev": "npm run build --workspace=lib-dataloader & npm run dev --workspace=timeseries"
  },
  "dependencies": {
    "carbon-components-svelte": "^0.70.13",
    "chroma-js": "^2.4.2",
    "concurrently": "^7.4.0"
  },
  "devDependencies": {
    "@types/chroma-js": "^2.1.4"
  }
}
